server:
  port: 9999

spring:
  application:
    name: wrappos
  thymeleaf:
    cache: false
    prefix: file:src/main/resources/templates/
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  datasource:
    url: jdbc:mariadb://localhost:3306/wrappos
    username: root
    password: 1111
    driver-class-name: org.mariadb.jdbc.Driver